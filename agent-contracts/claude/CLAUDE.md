# Claude Code-Specific Instructions

These instructions extend the canonical root guide and are loaded by `CLAUDE.md`.

Use the project skills exposed through `../../.claude/skills/` when their descriptions match the requested work.

Treat `../../skills/` as the canonical source for those skills. Do not edit files through the discovery link; update the canonical skill and its catalogue entries instead.
